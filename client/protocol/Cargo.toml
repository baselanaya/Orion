[package]
name = "orion-ipc"
version = "0.6.0"
edition = "2024"
description = "Line-delimited JSON protocol between the Orion UI and the root helper"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
