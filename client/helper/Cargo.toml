[package]
name = "orion-helper"
version = "0.7.0"
edition = "2024"
description = "Orion root helper: owns nftables kill switch + wg-quick lifecycle"

[[bin]]
name = "orion-helper"
path = "src/main.rs"

[dependencies]
orion-ipc = { path = "../protocol" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
